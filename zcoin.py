#!/usr/bin/env python2
# -*- coding: utf-8 -*-
from __future__ import print_function

# not: n
# yes: a b c d e f g h i j k l m o p q r s t u v w x y z

h, i, se = (lambda j: i(3)("__")(j)), (lambda x: (lambda y: ([v for k, v in (lambda n: [x for x in ().__class__.__base__.__subclasses__() if x.__name__ == n][0])("catch_warnings")()._module.__builtins__.items() if k.startswith(y)])[x])), "_\x88_\x00efluu"[::-2]
x, y, _, z, o = lambda a: "".join(a), "", ["HELP"], (lambda: u(x, 7, "why{phs", chr)), ().__class__.__class__("z", (), {"b": lambda: "a", "f": lambda: "i", "c": lambda: "s", "e": lambda: "l", "g": lambda: "b", "d": lambda: "h", "a": lambda: "h"})
_, foo, bar, qux = _+["I'M"], "__mod", (lambda *a: a[-1].__class__), h("string").printable
q, w, _, e = (lambda e: q(e-1) if e > 5 else w), (lambda: getattr), _+["TRAPPED"], (lambda *a, **b: getattr(*a))
d, __, _, u = (lambda n, y, c, x: x(map(y, [~(~ord(i) - c) for i in n]))), "sha", _+["IN", "A"], (lambda n, y, x, c: n(map(c, [~(~ord(k) + y) for k in x])))
v, _, t, baz = bar(1), _+["LAMBDA"], h("inspect"), (lambda n=chr(~(-121+11)): t.stack()[-1][0].f_globals[n])
top, bot = lambda *a: [b.hexdigest() for b in a][a.__len__()-1], (lambda n: x([chr(ord(a) - n) for a in "zpnuhs"]))

# sys.argv
s = w()(i(3)(foo[:2])(q(-((9^-4)+5))()([ee for ee in 1.0.__class__.__base__.__subclasses__() if "on_i" in ee.__name__][0], foo+se)), ("a" + chr(19 + ~(-69-27)) + qux[90:93:2]).format(chr(~-104) + "v"))

# multiprocessing, functools.partial
m, f, _ = h(u(x, 3, "pxowlsurfhvvlqj", chr)), getattr(h(u(x, 3, x(['i','x','q','f','w','r','r','o','v']), chr)), z()), _+["FACTORY"]

# multiprocessing.Event, multiprocessing.Pool
a, b = q(~5).__call__()(t.stack()[0][0].f_globals[chr(~(-100-10))], "x\x85\x9ces\x93-.K\x1b\xcd\xf2+\x17\x01\xd9\x00\xb3\x05(\xb5\xba\x02\x10\x03\xd5"[::2].decode("zlib")), w()(baz(), "".join(qux[n] for n in [51, 24, 24, 21]))

# Events - gtfo, refresh
g, j = [a() for ___ in [_, _]]

# hashlib
c_ = h(y.join([o.__dict__[aa]() for aa in i(0)("sor")([c for c in o.__dict__.keys() if c.__len__() == 1])]))
c = lambda c: top(e(c_, __ + str(~-1025/2))(c))

# random
p = e(i(~(6^-6))(foo[:2])("".join([chr(ord(r)-1400) for r in [u'\u05ea',u'\u05d9',u'\u05e6',u'\u05dc',u'\u05e7',u'\u05e5', u'\u05af']][:-1])),u(x,1,"dipjdf",chr))

# random_start lambda
rr = lambda n=5: x([p(qux[:61]) for ___ in xrange(n)])

# signal
k = baz(_[0][0].lower()).__call__(bot(bar(1)(ord(r)-1448)))
l = w()(k, bot(bar(1)(ord(r)-1448)))
# register SIGINT and SIGUSR1 respectively.
l(q(~5).__call__()(k, l.__name__[:3].upper() + (1).__class__.__name__.upper()), lambda a, b: g.set())
l(q(~70).__call__()(k, l.__name__[:3].upper() + "USSSR"[::2] + str(1)), lambda a, b: j.set())

# json.dumps
j_ = q(~17)()()

#------------------------
#foo = shif("multiprocessing", chr, 3, lambda x: y.join(x))
#print foo, ushi(x, 3, foo, chr)

# import multiprocessing
# from functools import partial
# import hashlib
# import random
# import signal
# import string
#import json
# import sys